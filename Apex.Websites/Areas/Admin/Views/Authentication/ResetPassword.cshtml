@{
    Layout = "~/Areas/Admin/Views/Authentication/_AuthenticationLayout.cshtml";
    ViewData["Title"] = "Reset Password";
}

@model Apex.Admin.ViewModels.Authentication.ResetPasswordViewModel

@section scripts {
    <script src="~/assets/scripts/admin/auth.min.js"></script>
}

<p class="login-box-msg">Reset your password</p>
<form asp-controller="authentication" asp-action="resetpassword" asp-anti-forgery="true" method="post" novalidate>
    <div asp-validation-summary="@ValidationSummary.ModelOnly" class="text-red"></div>
    <div class="form-group has-feedback">
        <input asp-for="Email" class="form-control" placeholder="Email">
        <span class="glyphicon glyphicon-envelope form-control-feedback"></span>
        <span asp-validation-for="Email" class="text-red"></span>
    </div>
    <div class="form-group has-feedback">
        <input asp-for="Password" class="form-control" placeholder="Password">
        <span class="glyphicon glyphicon-lock form-control-feedback"></span>
        <span asp-validation-for="Password" class="text-red"></span>
    </div>
    <div class="form-group has-feedback">
        <input asp-for="ConfirmPassword" class="form-control" placeholder="Confirm password">
        <span class="glyphicon glyphicon-log-in form-control-feedback"></span>
        <span asp-validation-for="ConfirmPassword" class="text-red"></span>
    </div>
    <div class="row">
        <div class="col-xs-offset-8 col-xs-4">
            <button type="submit" class="btn btn-primary btn-block btn-flat">Reset Password</button>
        </div>
    </div>
</form>
<a asp-controller="authentication" asp-action="signin" class="text-center">I already have a membership</a>
